import { notFound } from 'next/navigation'; import { prisma } from '@/lib/prisma'; import PlayerCard from '@/components/PlayerCard';
export default async function CardPage({params}:{params:{slug:string}}){const card=await prisma.playerCard.findUnique({where:{slug:params.slug}}); if(!card) notFound(); const updatedLabel=new Intl.DateTimeFormat('ru-RU',{dateStyle:'medium',timeStyle:'short',timeZone:'UTC'}).format(card.updatedAt); return <main><PlayerCard card={card}/><p>{card.description}</p><p>Updated: {updatedLabel} UTC</p></main>}
