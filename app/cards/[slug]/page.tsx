import { notFound } from 'next/navigation'; import { prisma } from '@/lib/prisma'; import PlayerCard from '@/components/PlayerCard';
export default async function CardPage({params}:{params:{slug:string}}){const raw=decodeURIComponent(params.slug); const card=await prisma.playerCard.findFirst({where:{OR:[{slug:raw},{slug:{equals:raw,mode:'insensitive'}}]}}); if(!card) notFound(); const updatedLabel=new Intl.DateTimeFormat('ru-RU',{dateStyle:'medium',timeStyle:'short',timeZone:'UTC'}).format(card.updatedAt); return <main><PlayerCard card={card}/><p>{card.description}</p><p>Updated: {updatedLabel} UTC</p></main>}
