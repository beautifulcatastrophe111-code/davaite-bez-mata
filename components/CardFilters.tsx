import { ranks, roles } from '@/lib/constants';

export default function CardFilters({searchParams}:{searchParams:Record<string,string|undefined>}){return <form className='surface mb-5 space-y-3'><input name='search' placeholder='Search by nickname or real name' defaultValue={searchParams.search||''} className='w-full'/><div className='grid md:grid-cols-3 gap-2'><select name='role' defaultValue={searchParams.role||''}><option value=''>All roles</option>{roles.map(r=><option key={r} value={r}>{r}</option>)}</select><select name='rank' defaultValue={searchParams.rank||''}><option value=''>All ranks</option>{ranks.map(r=><option key={r} value={r}>{r}</option>)}</select><select name='sort' defaultValue={searchParams.sort||'rating_desc'}><option value='rating_desc'>Rating (high → low)</option><option value='rating_asc'>Rating (low → high)</option><option value='newest'>Newest</option><option value='oldest'>Oldest</option><option value='nickname_asc'>Nickname (A-Z)</option></select></div><button type='submit'>Apply filters</button></form>}
