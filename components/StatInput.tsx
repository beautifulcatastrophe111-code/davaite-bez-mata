export default function StatInput({name,label,defaultValue=50}:{name:string;label:string;defaultValue?:number}){return <label className='block'><span>{label}: <b>{defaultValue}</b></span><input min={1} max={99} type='range' name={name} defaultValue={defaultValue} className='w-full'/></label>}
