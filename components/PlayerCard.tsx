import { PlayerCard as T } from '@prisma/client';
const styles={COMMON:'from-slate-700',RARE:'from-blue-700',EPIC:'from-purple-700',LEGENDARY:'from-amber-600',IMMORTAL:'from-red-700'};
const roleLabel=(role:string)=>role.toLowerCase().split('_').map((part)=>part.charAt(0).toUpperCase()+part.slice(1)).join(' ');
const flagEmoji=(code?:string|null)=>{const normalized=(code||'').toUpperCase(); if(!/^[A-Z]{2}$/.test(normalized)) return '🌍'; return String.fromCodePoint(...[...normalized].map((c)=>127397+c.charCodeAt(0)));};
export default function PlayerCard({card}:{card:T}){return <div className={`rounded-2xl p-5 w-[300px] h-[450px] flex flex-col justify-between bg-gradient-to-br ${styles[card.cardType]} to-slate-900 border border-slate-500 shadow-lg hover:shadow-xl transition-shadow`}><div className='flex justify-between items-start gap-3'><span className='text-2xl font-bold'>{card.rating}</span><span className='text-3xl'>{flagEmoji(card.country)}</span></div><div className='flex flex-col items-center justify-center gap-4 flex-1'><img src={card.photoUrl || 'https://placehold.co/160x160?text=Avatar'} alt={`${card.nickname} avatar`} className='w-36 h-36 rounded-full object-cover border-2 border-slate-300/30'/><div className='text-center'><b className='text-xl leading-none'>{card.nickname}</b><p className='text-sm text-slate-300 mt-1'>{roleLabel(card.role)}</p></div></div><div className='grid grid-cols-2 text-sm mt-3 gap-1'><span>LANE {card.skillLaning}</span><span>MICRO {card.skillMicro}</span><span>MACRO {card.skillMacro}</span><span>ITEMBUILDING {card.skillFarming}</span><span>POOL {card.skillHeroPool}</span><span>MENTALITY {card.skillMental}</span></div></div>}
